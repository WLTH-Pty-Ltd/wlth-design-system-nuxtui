// Playground — extends the local design system layer for development and visual testing.
// This app is NOT part of the published layer; it exists only for maintainer use.
export default defineNuxtConfig({
  extends: ['../']
})
